# Python ML 训练与服务依赖
# 用于 XGBoost 模型训练和 HTTP 推理服务

# 机器学习
xgboost>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
gensim>=4.3.0
torch>=2.0.0
networkx>=3.0

# HTTP 服务
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.31.0

# 对象存储（可选，用于模型注册和部署）
boto3>=1.28.0

# 训练数据加载：OSS Parquet、MySQL 协议（可选）
pyarrow>=14.0.0
s3fs>=2024.1.0
pymysql>=1.1.0

# 可观测性（生产级 /metrics）
prometheus_client>=0.19.0

# 测试依赖（可选）
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.25.0
